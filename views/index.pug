extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
     
      h1 Heisenbergs portfolio

     
      p.lead.text-light
        |The portfolio is showcasing various of fun projects. The purpose of the projects, is to use various javascript tools, to showcase some of my talent and skills.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

  
      div
        each val in projects
          div(class='cell')
          a(href= `projects/${val.id}`)
            img.thumbnail.image(src=`${val.image_urls}`)
            h5= `${val.project_name}`
            


